diff --git a/Website SEO/public_html/about-en.html b/Website SEO/public_html/about-en.html
index 8c0e42e..0f72869 100644
--- a/Website SEO/public_html/about-en.html	
+++ b/Website SEO/public_html/about-en.html	
@@ -751,6 +751,7 @@
             comfortSlider.addEventListener('input', function() {
                 comfortOutput.innerText = this.value;
             });
+        }
 
         // Main Audit Form Submit
         form.addEventListener('submit', async function(event) {
@@ -826,7 +827,8 @@
                 document.getElementById('welcome-message').textContent = `Welcome from ${country}!`;
             } catch (error) {
                 console.log('Country detection failed');
-
+            }
+        }
 
         // ==================== CHAT FUNCTIONS ====================
         async function handleSend() {
@@ -840,6 +842,7 @@
             if (!visitorId) {
                 visitorId = 'visitor_' + Date.now() + '_' + Math.random().toString(36).substr(2, 9);
                 localStorage.setItem('aidenVisitorId', visitorId);
+            }
 
             box.innerHTML += `<div class="bg-blue-900/50 ml-auto p-3 rounded-2xl rounded-br-none max-w-[85%] text-white">${message}</div>`;
             input.value = '';
diff --git a/Website SEO/public_html/about.html b/Website SEO/public_html/about.html
index 0331774..2d6b3c3 100644
--- a/Website SEO/public_html/about.html	
+++ b/Website SEO/public_html/about.html	
@@ -740,6 +740,7 @@
             comfortSlider.addEventListener('input', function() {
                 comfortOutput.innerText = this.value;
             });
+        }
 
         // Main Audit Form Submit
         form.addEventListener('submit', async function(event) {
@@ -816,7 +817,8 @@
                 document.getElementById('welcome-message').textContent = `مرحباً بك من ${country}!`;
             } catch (error) {
                 console.log('Country detection failed');
-
+            }
+        }
 
         // ==================== CHAT FUNCTIONS ====================
         async function handleSend() {
@@ -830,6 +832,7 @@
             if (!visitorId) {
                 visitorId = 'visitor_' + Date.now() + '_' + Math.random().toString(36).substr(2, 9);
                 localStorage.setItem('aidenVisitorId', visitorId);
+            }
 
             box.innerHTML += `<div class="bg-blue-900/50 mr-auto p-3 rounded-2xl rounded-bl-none max-w-[85%] text-white">${message}</div>`;
             input.value = '';
diff --git a/Website SEO/public_html/contact-en.html b/Website SEO/public_html/contact-en.html
index 8790fd4..8708bb8 100644
--- a/Website SEO/public_html/contact-en.html	
+++ b/Website SEO/public_html/contact-en.html	
@@ -684,6 +684,7 @@
             comfortSlider.addEventListener('input', function() {
                 comfortOutput.innerText = this.value;
             });
+        }
 
         // Main Audit Form Submit
         form.addEventListener('submit', async function(event) {
@@ -779,14 +780,14 @@
                     } else {
                         alert('Something went wrong. Please try again or use the call booking form.');
                     }
-                }
                 } catch (error) {
                     alert('Connection error. Please try again.');
                 } finally {
                     submitBtn.textContent = originalText;
                     submitBtn.disabled = false;
-
+                }
             });
+        }  // end if (quickForm)
 
         modal.addEventListener('click', (event) => {
             if (event.target === modal) closeAuditForm();
@@ -805,7 +806,8 @@
                 document.getElementById('welcome-message').textContent = `Welcome from ${country}!`;
             } catch (error) {
                 console.log('Country detection failed');
-
+            }
+        }
 
         // ==================== CHAT FUNCTIONS ====================
         async function handleSend() {
@@ -819,6 +821,7 @@
             if (!visitorId) {
                 visitorId = 'visitor_' + Date.now() + '_' + Math.random().toString(36).substr(2, 9);
                 localStorage.setItem('aidenVisitorId', visitorId);
+            }
 
             box.innerHTML += `<div class="bg-blue-900/50 ml-auto p-3 rounded-2xl rounded-br-none max-w-[85%] text-white">${message}</div>`;
             input.value = '';
diff --git a/Website SEO/public_html/contact.html b/Website SEO/public_html/contact.html
index 23d6398..3179a09 100644
--- a/Website SEO/public_html/contact.html	
+++ b/Website SEO/public_html/contact.html	
@@ -673,6 +673,7 @@
             comfortSlider.addEventListener('input', function() {
                 comfortOutput.innerText = this.value;
             });
+        }
 
         // Main Audit Form Submit
         form.addEventListener('submit', async function(event) {
@@ -794,7 +795,8 @@
                 document.getElementById('welcome-message').textContent = `مرحباً بك من ${country}!`;
             } catch (error) {
                 console.log('Country detection failed');
-
+            }
+        }
 
         // ==================== CHAT FUNCTIONS ====================
         async function handleSend() {
@@ -808,6 +810,7 @@
             if (!visitorId) {
                 visitorId = 'visitor_' + Date.now() + '_' + Math.random().toString(36).substr(2, 9);
                 localStorage.setItem('aidenVisitorId', visitorId);
+            }
 
             // RTL: user message on the right (mr-auto)
             box.innerHTML += `<div class="bg-blue-900/50 mr-auto p-3 rounded-2xl rounded-bl-none max-w-[85%] text-white">${message}</div>`;
@@ -853,7 +856,7 @@
                     }
                 }, 10000);
             }
-        };
+        });
 
         // ==================== FIXED: MOBILE MENU TOGGLE (Unified System) ====================
         const mobileToggleBtn = document.getElementById('mobileMenuToggle');
@@ -869,16 +872,15 @@
             mobileMenuDropdown.querySelectorAll('a').forEach(link => {
                 link.addEventListener('click', () => {
                     mobileMenuDropdown.classList.remove('open');
-                    }
                 });
-            }
-        });
-            
+            });
+        }
+
             // Close if user clicks outside
             document.addEventListener('click', function(event) {
                 if (!mobileToggleBtn.contains(event.target) && !mobileMenuDropdown.contains(event.target)) {
                     mobileMenuDropdown.classList.remove('open');
-
+                }
             });
 
         // ==================== INIT ====================
@@ -889,10 +891,11 @@
                     if (!document.getElementById('aiden-ui').classList.contains('active')) {
                         toggleChat();
                         sessionStorage.setItem('aidenPopped', 'true');
-
+                    }
                 }, 10000);
-
+            }
         });
+        }  // end if (quickForm)
     </script>
 <script>
         // STANDARD NAV JS
diff --git a/Website SEO/public_html/process-en.html b/Website SEO/public_html/process-en.html
index 6fae8a8..f7a7b2d 100644
--- a/Website SEO/public_html/process-en.html	
+++ b/Website SEO/public_html/process-en.html	
@@ -798,6 +798,7 @@
             comfortSlider.addEventListener('input', function() {
                 comfortOutput.innerText = this.value;
             });
+        }
 
         // Form Submit
         form.addEventListener('submit', async function(event) {
@@ -873,7 +874,8 @@
                 document.getElementById('welcome-message').textContent = `Welcome from ${country}!`;
             } catch (error) {
                 console.log('Country detection failed');
-
+            }
+        }
 
         // ==================== CHAT FUNCTIONS ====================
         async function handleSend() {
@@ -887,6 +889,7 @@
             if (!visitorId) {
                 visitorId = 'visitor_' + Date.now() + '_' + Math.random().toString(36).substr(2, 9);
                 localStorage.setItem('aidenVisitorId', visitorId);
+            }
 
             box.innerHTML += `<div class="bg-blue-900/50 ml-auto p-3 rounded-2xl rounded-br-none max-w-[85%] text-white">${message}</div>`;
             input.value = '';
diff --git a/Website SEO/public_html/process.html b/Website SEO/public_html/process.html
index 43643b9..1fae200 100644
--- a/Website SEO/public_html/process.html	
+++ b/Website SEO/public_html/process.html	
@@ -728,31 +728,6 @@
 
     <!-- ======================= SCRIPT (Fixed Mobile Menu Logic) ======================= -->
     <script>
-        
-
-                // Close menu when clicking a link
-                mobileMenu.querySelectorAll('a').forEach(link => {
-                    link.addEventListener('click', function() {
-                        mobileMenu.classList.remove('active');
-                    });
-                });
-
-                // Close when clicking outside
-                document.addEventListener('click', function(event) {
-                    if (!mobileMenu.contains(event.target) && !toggleBtn.contains(event.target)) {
-                        mobileMenu.classList.remove('active');
-                    }
-                });
-
-                // Optional: close on escape key
-                document.addEventListener('keydown', function(e) {
-                    if (e.key === 'Escape' && mobileMenu.classList.contains('active')) {
-                        mobileMenu.classList.remove('active');
-                    }
-                });
-
-        });
-
         // ==================== MULTI-STEP FORM LOGIC ====================
         const modal = document.getElementById('auditModal');
         const form = document.getElementById('auditForm');
@@ -799,14 +774,14 @@
                     if (currentStep < steps.length - 1) {
                         currentStep++;
                         showStep(currentStep);
-
-
+                    }
+                }
             } else if (e.target.matches('.prev')) {
                 if (currentStep > 0) {
                     currentStep--;
                     showStep(currentStep);
-
-
+                }
+            }
         });
 
         // Slider
@@ -817,6 +792,7 @@
             comfortSlider.addEventListener('input', function() {
                 comfortOutput.innerText = this.value;
             });
+        }
 
         // Form Submit
         form.addEventListener('submit', async function(event) {
@@ -866,13 +842,13 @@
                     window.location.href = "https://calendly.com/ai-profit-lab2026";
                 } else {
                     alert("حدث خطأ. الرجاء المحاولة مرة أخرى.");
-
+                }
             } catch (error) {
                 alert("خطأ في الاتصال. تحقق من اتصالك بالإنترنت.");
             } finally {
                 submitBtn.textContent = originalText;
                 submitBtn.disabled = false;
-
+            }
         });
 
         modal.addEventListener('click', (event) => {
@@ -892,7 +868,8 @@
                 document.getElementById('welcome-message').textContent = `مرحباً بك من ${country}!`;
             } catch (error) {
                 console.log('Country detection failed');
-
+            }
+        }
 
         // ==================== CHAT FUNCTIONS ====================
         async function handleSend() {
@@ -906,6 +883,7 @@
             if (!visitorId) {
                 visitorId = 'visitor_' + Date.now() + '_' + Math.random().toString(36).substr(2, 9);
                 localStorage.setItem('aidenVisitorId', visitorId);
+            }
 
             box.innerHTML += `<div class="bg-blue-900/50 mr-auto p-3 rounded-2xl rounded-bl-none max-w-[85%] text-white">${message}</div>`;
             input.value = '';
