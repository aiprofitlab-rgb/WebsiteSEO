node_modules/
# Real keys and tokens — never in git. This repo is public.
.env
.env.*
!.env.example
*.sqlite
*.sqlite-journal
# WAL mode writes these two beside the database; they are live state, not code.
*.sqlite-wal
*.sqlite-shm
token.json
sa.json

# Written at runtime by the admin panel. The repo's rules.json is only the seed.
uploads/
rules-backups/
